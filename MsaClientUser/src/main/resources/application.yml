spring:
  profiles:
    active: dev
---
server:
  port: 8000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bank_user?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: hanif1293@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

  profiles: dev
  application:
    name: eureka_client_user

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true